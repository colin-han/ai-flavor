# 回归跑测记录

每次回归产出一份 `<YYYY-MM-DD>-<case-id>.md`（全量回归用 `-baseline`）。

> **历史路径说明**：2026-09-09 的目录重构把 `regression/cases/` 拆成了 `quantitative/` 与 `qualitative/`，`references/rules.md` 移到了 `rules/rules.md`。重构之前的记录（`2026-09-07-*.md`）**保留原样不改**——历史记录应当反映当时的真实路径。
